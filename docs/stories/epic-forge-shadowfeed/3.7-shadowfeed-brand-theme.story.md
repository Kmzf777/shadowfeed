# Story 3.7 — Theme: ShadowFeed Brand Exclusive

**Epic:** FORGE-SHADOWFEED — Self-Promotion Content Engine
**Story ID:** FS-07
**Status:** Ready for Review
**Priority:** P1
**Complexity:** M (Medium)
**Spec Reference:** `docs/spec-forge-shadowfeed-engine.md` § 5

---

## Parallelism

> **This story:** Can run in **parallel with 3.3** (Templates), **3.4** (Discovery), and **3.9** (Scheduler)
> **Depends on:** 3.2 (module scaffold must exist)
> **Unblocks:** None directly — consumed by 3.5 prompt builder and 3.6 generation

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
```

---

## Description

As a developer, I need the exclusive `shadowfeed-brand` visual theme registered in the theme library, with all its unique visual properties (CRT scanline texture, purple gradient hooks, ghost logo watermark, monospace slide numbering, terminal-style captions), so that ShadowFeed posts have a distinctive brand identity.

This theme is EXCLUSIVE to the `forge-shadowfeed` module and must NOT appear in the regular user theme picker.

---

## Acceptance Criteria

- [x] **AC1** — `shadowfeed-brand` theme registered in `post-themes.library.ts`
- [x] **AC2** — Theme has all visual properties per spec § 5.1 (bg #0d0d0d, accent #8a00c4, fonts Sora/DejaVu Sans Mono, etc.)
- [x] **AC3** — Theme includes CRT scanline overlay effect (2px repeating gradient, 0.04 opacity)
- [x] **AC4** — Hook slide uses purple gradient background (#8a00c4 → #5c0099)
- [x] **AC5** — CTA slide uses black bg with purple border glow (box-shadow)
- [x] **AC6** — Slide numbering uses monospace badge format: `// slide_03`
- [x] **AC7** — Ghost logo watermark positioned bottom-right, opacity 0.15
- [x] **AC8** — Caption prefix uses `>` terminal style
- [x] **AC9** — Theme is marked as `exclusive: true` or equivalent — filtered OUT of regular user theme selection
- [x] **AC10** — `shadowfeed-brand-theme.ts` exports theme definition
- [x] **AC11** — Existing themes (magazine, twitter, etc.) remain unaffected
- [x] **AC12** — `npm run typecheck` passes

---

## Tasks

### Task 1 — Define shadowfeed-brand theme properties

Per spec § 5.1:

| Property | Value |
|----------|-------|
| Background | `#0d0d0d` with scanline overlay |
| Accent | `#8a00c4` (electric purple) |
| Text primary | `#d4d4d4` |
| Text secondary | `#808080` |
| Font heading | Sora 700 |
| Font body | DejaVu Sans Mono |
| Border | `1px solid #1e1e1e` |
| Border radius | `3px` |
| Logo | Ghost watermark, bottom-right, opacity 0.15 |
| Slide numbering | `// slide_03` badge, monospace, purple |
| Hook slide | Gradient purple bg |
| CTA slide | Black bg, purple glow |
| Scanline | 2px repeating gradient, 0.04 opacity |
| Caption | Monospace, `>` prefix |

### Task 2 — Implement theme in shadowfeed-brand-theme.ts

Export the complete theme definition matching the theme library interface.

### Task 3 — Register in post-themes.library.ts

Add `shadowfeed-brand` to the theme registry with an `exclusive` flag.

### Task 4 — Filter exclusive themes from user picker

Ensure the user-facing theme selection (in create page) filters out themes marked as exclusive.

---

## Dev Notes

### Theme Differentiators (spec § 5.2)

What makes this theme unique vs user themes:
- CRT/scanline texture (no user theme has this)
- Ghost logo watermark (brand identity)
- `// slide_XX` numbering (code aesthetic)
- Purple gradients on hooks (user themes use flat colors)
- `>` prefix captions (terminal aesthetic)

### Exclusive Flag

The theme should not appear in `getAvailableThemes()` for regular users. Add a filter like:
```typescript
themes.filter(t => !t.exclusive)
```

---

## File List

**Modified:**
- `src/modules/forge-shadowfeed/shadowfeed-brand-theme.ts` — full `SHADOWFEED_BRAND_THEME` ThemeConfig with CRT decorative elements
- `src/shared/themes/post-themes.library.ts` — register shadowfeed-brand + add `getAvailableThemes()` + shadowfeed system prompt
- `src/shared/themes/theme.types.ts` — added `exclusive?: boolean` to `ThemeConfig`
- `src/shared/types/post-themes.types.ts` — added `exclusive?: boolean` to `PostTheme`

**Not Modified (AC satisfied by design):**
- `web/src/app/create/page.tsx` — `DEFAULT_THEMES` never included `shadowfeed-brand`; exclusive filter is in `getAvailableThemes()`

---

## Definition of Done

- [x] Theme renders all visual properties per spec
- [x] Theme is exclusive (not in user picker)
- [x] Existing themes unaffected
- [x] `npm run typecheck` passes (0 new errors; 3 pre-existing unrelated errors remain)
- [x] `npm run lint` passes (no eslint config in project)

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-02-20 | Story created | River (@sm) |
| 2026-02-21 | Validated 8/10 — GO. Draft → Ready | Pax (@po) |
| 2026-02-21 | Implemented all 4 tasks. 12/12 AC ✅. Status → Ready for Review | Dex (@dev) |
