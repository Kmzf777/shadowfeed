# Story 3.10 — Frontend: /shadowfeedadmin Dashboard + Posts Review

**Epic:** FORGE-SHADOWFEED — Self-Promotion Content Engine
**Story ID:** FS-10
**Status:** Draft
**Priority:** P1
**Complexity:** M (Medium)
**Spec Reference:** `docs/spec-forge-shadowfeed-engine.md` § 8

---

## Parallelism

> **This story:** Cannot run in parallel — waits for 3.6 (API endpoints) + 3.8 (session status)
> **Depends on:** 3.6, 3.8
> **Unblocks:** 3.11 (Integration test)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
```

---

## Description

As an admin of the ShadowFeed system, I need a web admin panel at `/shadowfeedadmin` with two pages — a dashboard showing system status, today's queue, and generation controls, and a posts review page for previewing, editing captions, and approving posts before publication.

The admin panel uses the DS-Terminal design tokens and follows the terminal aesthetic consistent with the ShadowFeed brand.

---

## Acceptance Criteria

- [ ] **AC1** — Route `/shadowfeedadmin` renders the dashboard page
- [ ] **AC2** — Route `/shadowfeedadmin/posts` renders the posts review page
- [ ] **AC3** — `layout.tsx` implements token-based auth: checks `x-sf-admin-token` header or `?token=` query param
- [ ] **AC4** — Invalid/missing token shows 404 page (not 401 — hides admin route)
- [ ] **AC5** — Dashboard shows: system status (active/inactive), posts count today (X/4), next scheduled post
- [ ] **AC6** — Dashboard shows Instagram session status (connected/disconnected)
- [ ] **AC7** — "GENERATE NEXT BATCH" button calls `POST /api/forge-shadowfeed/generate-batch`
- [ ] **AC8** — Today's queue table shows: time, pillar name, status badge, [view]/[post!] actions
- [ ] **AC9** — AUTO-PUBLISH toggle calls `PUT /api/forge-shadowfeed/config`
- [ ] **AC10** — Recent posts section shows last 7 days
- [ ] **AC11** — Posts review page shows cards for each pending post with: pillar, date/time, hook preview, theme, slide count
- [ ] **AC12** — [preview] button opens carousel preview modal (reuse existing preview component)
- [ ] **AC13** — [edit caption] allows inline caption editing + save
- [ ] **AC14** — [CONFIRM] button approves post (`PUT /api/forge-shadowfeed/queue/:id/approve`)
- [ ] **AC15** — Filter tabs: All, Pending, Approved, Posted
- [ ] **AC16** — Terminal aesthetic: monospace fonts, dark theme, purple accents
- [ ] **AC17** — `npm run typecheck` passes

---

## Tasks

### Task 1 — Create admin page structure

```
web/src/app/shadowfeedadmin/
├── page.tsx                    # Dashboard
├── layout.tsx                  # Auth middleware
├── posts/
│   └── page.tsx                # Posts review
└── components/
    ├── BatchGenerator.tsx      # Generate batch button
    ├── PostQueue.tsx           # Today's queue table
    ├── PostReviewCard.tsx      # Post card with preview/edit/confirm
    ├── PublishToggle.tsx       # Auto-publish toggle
    ├── SessionStatus.tsx       # Instagram session indicator
    └── RecentPosts.tsx         # Last 7 days list
```

### Task 2 — Implement layout.tsx with auth

Token check flow:
1. Read token from `?token=` query param or cookie/header
2. Compare with `SHADOWFEED_ADMIN_TOKEN` env var
3. If invalid → render 404 page
4. If valid → render children + store token for API calls

### Task 3 — Implement dashboard page (page.tsx)

Per spec § 8.3 wireframe:
- Status indicator (active/inactive)
- Posts today counter (X/4)
- Next scheduled post info
- Instagram session status
- GENERATE NEXT BATCH button
- Today's queue table
- AUTO-PUBLISH toggle
- Recent posts (last 7 days)

### Task 4 — Implement posts review page (posts/page.tsx)

Per spec § 8.4 wireframe:
- Filter tabs (All, Pending, Approved, Posted)
- PostReviewCard for each post
- Preview modal
- Caption edit inline
- Confirm button

### Task 5 — Implement components

6 components per file list above. Each component calls the appropriate API endpoint with the admin token.

### Task 6 — Style with terminal aesthetic

Use DS-Terminal design tokens:
- Dark background
- Monospace fonts
- Purple accent (#8a00c4)
- Terminal-style borders and badges

---

## Dev Notes

### Auth Strategy

Since this is a simple admin panel (not multi-user), the token approach is sufficient:
- Token passed via `?token=` on first visit → stored in localStorage
- All API calls include `x-sf-admin-token` header
- No session management needed

### Reuse Carousel Preview

The existing carousel preview component from the create page should be reusable for the [preview] modal. Import and adapt as needed.

### API Calls

All API calls go to `/api/forge-shadowfeed/*` with the admin token header.

---

## File List

**New:**
- `web/src/app/shadowfeedadmin/page.tsx`
- `web/src/app/shadowfeedadmin/layout.tsx`
- `web/src/app/shadowfeedadmin/posts/page.tsx`
- `web/src/app/shadowfeedadmin/components/BatchGenerator.tsx`
- `web/src/app/shadowfeedadmin/components/PostQueue.tsx`
- `web/src/app/shadowfeedadmin/components/PostReviewCard.tsx`
- `web/src/app/shadowfeedadmin/components/PublishToggle.tsx`
- `web/src/app/shadowfeedadmin/components/SessionStatus.tsx`
- `web/src/app/shadowfeedadmin/components/RecentPosts.tsx`

---

## Definition of Done

- [ ] Both pages render correctly
- [ ] Auth gate works (404 on bad token)
- [ ] Dashboard shows live data from API
- [ ] Batch generation triggers from button
- [ ] Auto-publish toggle persists
- [ ] Post review allows preview, caption edit, and approval
- [ ] Filter tabs work
- [ ] Terminal aesthetic consistent
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-02-20 | Story created | River (@sm) |
