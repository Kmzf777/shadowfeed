# Story 3.11 — Integration: End-to-End Test + Polish

**Epic:** FORGE-SHADOWFEED — Self-Promotion Content Engine
**Story ID:** FS-11
**Status:** Draft
**Priority:** P2
**Complexity:** M (Medium)
**Spec Reference:** `docs/spec-forge-shadowfeed-engine.md` § 10, § 14, § 15

---

## Parallelism

> **This story:** Cannot run in parallel — final integration, depends on ALL previous stories (3.1–3.10)
> **Depends on:** 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10
> **Unblocks:** None (epic complete)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
```

---

## Description

As a developer completing the Forge ShadowFeed epic, I need to verify the complete end-to-end flow works (batch generation → queue → approval → scheduled publishing), implement the initial Instagram session setup flow, add error recovery, and polish the integration points between all modules.

This is the final story that proves the system works as a whole.

---

## Acceptance Criteria

- [ ] **AC1** — End-to-end: "Generate Batch" produces 4 valid carousels with correct pillar assignments
- [ ] **AC2** — End-to-end: Queue shows all 4 posts with correct scheduled times (09:00, 13:00, 17:00, 20:00 BRT)
- [ ] **AC3** — End-to-end: Approving a post changes status from `ready` → `approved`
- [ ] **AC4** — End-to-end: Caption editing persists correctly
- [ ] **AC5** — End-to-end: Scheduler detects approved + due posts and triggers publisher
- [ ] **AC6** — End-to-end: Publisher posts carousel to Instagram (or logs attempt in test mode)
- [ ] **AC7** — Instagram session setup flow: admin can authenticate via headed browser
- [ ] **AC8** — Session persistence: authenticated session survives server restart
- [ ] **AC9** — Error recovery: failed generation for one pillar doesn't block others
- [ ] **AC10** — Error recovery: failed publish sets status to `failed` with actionable error message
- [ ] **AC11** — Discovery fallback: when no trends found, hardcoded templates are used (never empty)
- [ ] **AC12** — Theme rotation: across 10 generated posts, ~70% use `shadowfeed-brand`, ~30% use regular themes
- [ ] **AC13** — Template rotation: no template repeated within 7-day window
- [ ] **AC14** — All env vars documented in `.env.example` or similar
- [ ] **AC15** — `npm run typecheck` passes
- [ ] **AC16** — `npm run lint` passes

---

## Tasks

### Task 1 — Implement session setup flow

Add endpoint or admin UI button to:
1. Launch Playwright in headed mode
2. Navigate to Instagram login
3. Wait for admin to log in manually
4. Save session state to `SHADOWFEED_SESSION_DIR`
5. Confirm session is valid

### Task 2 — End-to-end smoke test

Manual or semi-automated test of full flow:
1. Generate batch for tomorrow
2. Verify 4 queue items created
3. Preview each carousel
4. Edit one caption
5. Approve all 4
6. Verify scheduler picks them up at correct time
7. Verify publisher posts (or simulates posting)

### Task 3 — Error recovery verification

Test failure scenarios:
- Discovery API timeout → falls back to templates
- LLM generation failure for one pillar → others still generate
- Playwright session expired → status `failed`, clear error message
- Instagram DOM changed → graceful failure with actionable error

### Task 4 — Rotation verification

Generate 10+ posts across multiple batches:
- Verify template rotation (no 7-day repeats)
- Verify theme rotation (~70/30 ratio)
- Verify caption style rotation (follows pillar preference)
- Verify hashtag rotation (2 fixed + 3 from pool)

### Task 5 — Environment documentation

Document all new env vars:
```
SHADOWFEED_ADMIN_TOKEN=sf_admin_<random-64-chars>
SHADOWFEED_INSTAGRAM_HANDLE=@shadowfeed.ai
SHADOWFEED_TIMEZONE=America/Sao_Paulo
TWITTER_IO_API_KEY=<key>
SHADOWFEED_SESSION_DIR=.playwright/sessions/shadowfeed-ig
```

### Task 6 — Integration polish

- Ensure all error messages are actionable
- Verify admin panel shows correct real-time data
- Verify stats endpoint returns accurate metrics
- Clean up any TODO/FIXME comments left during development

---

## Dev Notes

### Test Mode

For CI/testing, the publisher should support a `DRY_RUN` mode that logs the publishing action without actually posting to Instagram. Check for `NODE_ENV === 'test'` or a `SHADOWFEED_DRY_RUN=true` env var.

### Metrics Baseline

After first successful batch, verify stats endpoint returns:
- `generation_time_ms` per post
- `pillar_id` distribution
- `theme_used` ratio
- `status` progression rates

---

## File List

**Modified:**
- Various files across `forge-shadowfeed` and `shadowfeed-publisher` modules (bug fixes, polish)
- `src/modules/forge-shadowfeed/forge-shadowfeed.controller.ts` (session setup endpoint)

**New:**
- `.env.example` or update existing with new vars

---

## Definition of Done

- [ ] Full end-to-end flow works (generate → approve → publish)
- [ ] Session setup flow documented and working
- [ ] Error recovery tested for all failure scenarios
- [ ] Rotation logic verified (templates, themes, captions, hashtags)
- [ ] All env vars documented
- [ ] No TODO/FIXME left in code
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-02-20 | Story created | River (@sm) |
