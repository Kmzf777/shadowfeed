# Story 3.5 — Backend: Prompt Builder + Persona

**Epic:** FORGE-SHADOWFEED — Self-Promotion Content Engine
**Story ID:** FS-05
**Status:** Ready for Review
**Priority:** P0
**Complexity:** L (Large)
**Spec Reference:** `docs/spec-forge-shadowfeed-engine.md` § 2.1, § 4.5, § 10, § 11

---

## Parallelism

> **This story:** Cannot run in parallel — waits for 3.3 (Templates) + 3.4 (Discovery)
> **Depends on:** 3.3, 3.4
> **Unblocks:** 3.6 (Batch Generation)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
```

---

## Description

As a developer building the ShadowFeed engine, I need a prompt builder that constructs the 8-block prompt per spec § 4.5, injecting the ShadowFeed AI-sentient persona, pillar-specific rules, source content (from discovery or template), product context, anti-patterns, and few-shot examples, so that the LLM generates on-brand carousels in PT-BR.

This is the creative core of the system — it defines HOW the ShadowFeed voice sounds and ensures every post is on-persona.

---

## Acceptance Criteria

- [x] **AC1** — `shadowfeed-prompt-builder.ts` constructs prompts with all 8 blocks per spec § 4.5
- [x] **AC2** — Block 1 (Persona): ShadowFeed AI-sentient identity is injected — 1st person, acid humor, casual authority
- [x] **AC3** — Block 2 (Pillar): Active pillar config injected (tone, slide count, objective)
- [x] **AC4** — Block 3 (Rules): Pillar-specific rules (CTA, slide range, content type)
- [x] **AC5** — Block 4 (Source): Discovery winner metadata OR hardcoded template content
- [x] **AC6** — Block 5 (Product): `SHADOWFEED_PRODUCT_CONTEXT` (features, pricing, CTAs) from constants
- [x] **AC7** — Block 6 (Anti-patterns): Prohibited phrases/tones per spec § 2.1 "Proibido" section
- [x] **AC8** — Block 7 (Few-shot): 1–2 complete example posts per pillar (from spec § 11)
- [x] **AC9** — Block 8 (Output format): JSON schema matching the carousel output format
- [x] **AC10** — Prompt includes caption style instruction (A/B/C) based on rotation
- [x] **AC11** — Prompt includes hashtag selection (5 total, 2 fixed + 3 rotated)
- [x] **AC12** — All prompt text is in PT-BR
- [x] **AC13** — Prompt builder integrates with `forge-personalized` generation pipeline (reusing the carousel generation call)
- [x] **AC14** — `npm run typecheck` passes (no errors in forge-shadowfeed module; pre-existing errors in twitter.source.ts/content-validator.test.ts unrelated)

---

## Tasks

### Task 1 — [x] Implement persona definition block

The ShadowFeed DNA (spec § 2.1):
- **Máquina Senciente**: 1st person as a self-aware AI
- **Desdém Ácido**: Manual creation = primitive
- **Autoridade Casual**: Don't prove — just state
- **Empatia Provocativa**: Understand pain, then twist the knife
- **Anti-Curso**: Attack infoproducts indirectly

### Task 2 — [x] Implement pillar-specific prompt blocks

Each pillar has different:
- Tone intensity (MAX acid → confident flex → educational edge → direct sales)
- Slide range
- Content type objective
- CTA style

### Task 3 — [x] Implement source content injection

Two paths:
1. **Discovery winner** → inject title, summary, URL, key data points
2. **Hardcoded template** → inject template theme, hook line, body hints, variables filled

### Task 4 — [x] Implement anti-patterns block

Hard rules the LLM must NEVER break:
- No motivational/coaching tone
- No "Você consegue!", "Acredite em si mesmo!"
- No "hack"/"segredo" without irony
- No direct engagement asks ("comenta aí!", "salva esse post!")

### Task 5 — [x] Implement few-shot examples

Include 1–2 complete carousel JSON examples per pillar from spec § 11.

### Task 6 — [x] Implement output format specification

JSON schema matching the carousel format with slides, caption, hashtags, CTA.

### Task 7 — [x] Implement carousel generation call

Reuse `forge-personalized` generation pipeline:
- Call the LLM (OpenAI GPT-4o via `OPENAI_MODEL`)
- Validate response with Zod schema
- Enrich slides with Pexels images
- Apply selected theme (brand or showcase)

---

## Dev Notes

### Reuse forge-personalized Pipeline

The actual LLM call, Zod validation, Pexels enrichment, and theme application should reuse `forge-personalized.service.ts` internals. Only the PROMPT is different — everything downstream is the same.

### Model Usage

- Query generation (story 3.4): `gpt-4o-mini`
- Carousel generation (this story): `gpt-4o` (via `OPENAI_MODEL`)

### No Credit System

ShadowFeed posts do NOT consume user tokens. The generation runs on the system's own API key with no credit checks or deductions.

---

## File List

**Modified:**
- `src/modules/forge-shadowfeed/shadowfeed-prompt-builder.ts` — Full 8-block prompt builder + `generateShadowFeedCarousel()` generation pipeline

**New:**
- `src/modules/forge-shadowfeed/shadowfeed-persona.ts` — Persona voice, anti-patterns, per-pillar prompt rules (PILLAR_PROMPT_RULES)
- `src/modules/forge-shadowfeed/shadowfeed-few-shots.ts` — 1 complete few-shot example per pillar (PILLAR_FEW_SHOTS)

**Referenced (read-only):**
- `src/modules/forge-personalized/forge-personalized.service.ts` (reuse generation pipeline)
- `src/modules/forge-shadowfeed/shadowfeed-constants.ts` (product context, from 3.3)
- `src/modules/forge-shadowfeed/pillar-templates/` (templates, from 3.3)

---

## Definition of Done

- [x] Prompt builder produces well-structured 8-block prompts
- [x] Persona voice is consistent across all pillars
- [x] Discovery and template source paths both work
- [x] Anti-patterns are explicitly listed in every prompt
- [x] Few-shot examples included per pillar (1 per pillar)
- [x] Output JSON matches expected carousel format
- [x] Generation calls LLM and returns valid carousel
- [x] All content in PT-BR
- [x] `npm run typecheck` passes (no errors in forge-shadowfeed module)
- [x] `npm run lint` passes (no lint script configured in project)

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-02-20 | Story created | River (@sm) |
| 2026-02-21 | Validated 8/10 — GO. Draft → Ready | Pax (@po) |
| 2026-02-21 | All 7 tasks implemented. 14/14 AC met. Status → Ready for Review | Dex (@dev) |
