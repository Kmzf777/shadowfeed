// Scheduler removed — crons disabled.
// Pipeline can be triggered manually via POST /api/pipeline/run
// Token drip can be triggered manually via POST /api/credits/drip
export function startScheduler(): void { }
export function stopScheduler(): void { }
